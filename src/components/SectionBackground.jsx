const SectionBackground = () => {
  return (
    <div className="fixed inset-0 w-full h-full pointer-events-none">
      {/* Background removed to allow global pure black theme */}
    </div>
  );
};

export default SectionBackground;